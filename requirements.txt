# core pipeline (stages 1-3, and all of tests/benchmark.py)
numpy>=1.24
opencv-python>=4.8
scipy>=1.10
pandas>=2.0

# learned model (stages 4-5) -- preinstalled on Colab
# torch>=2.0

# ffmpeg must be on PATH for decoding and the synthetic re-encode step.
